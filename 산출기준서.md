# 산출 기준서 (Methodology)

> 플랫폼 내 「산출 기준」 페이지에 그대로 넣을 내용.
> 각 화면의 숫자 옆 ⓘ 아이콘에서 해당 항목으로 바로 이동.

---

## 0. 이 문서가 있는 이유

이 플랫폼은 데이터의 신뢰도를 판정한다. 그렇다면 **판정 기준 자체도 검증 가능해야 한다.**

아래 모든 수치에 출처 등급을 표시했다. 확정된 것과 가정한 것을 구분하지 않으면,
우리가 다른 데이터에 요구하는 것을 우리 스스로 지키지 않는 셈이 된다.

---

## 1. 통합단말기 데이터 기준 산정

### 1-1. 정규화

원시 이벤트 건수는 그대로 쓰지 않는다. 많이 달리면 많이 발생하기 때문이다.

```
발생률 = (급가속 + 급출발 + 급감속 + 급정지) ÷ 주행거리(km) × 100
       →  100km당 발생 횟수
```

공차 구간과 적차 구간은 운전 난이도가 다르므로 각각 산출한 뒤 주행거리로 가중 합산한다.

**과속은 별도로 둔다.** 현재 수집되는 데이터에서 과속 항목이 전 차량 0으로 보고되고 있어,
준수 실적이 아니라 미수집 상태로 판단하고 발생률 계산에서 제외한다.

### 1-2. 신뢰등급 판정

| 등급 | 조건 | 처리 |
|---|---|---|
| 정상 | 아래 어디에도 해당하지 않음 | 이벤트 기준 평가 |
| A 센서 과민 | 발생률 > 100km당 100회 | 이벤트 폐기 → 연료 기준 대체 |
| B 센서 둔감 | 이벤트와 연료 원단위가 역행 | 이벤트 폐기 → 연료 기준 대체 |
| C 센서 침묵 | 발생률 = 0 이고 주행거리 > 100km | 이벤트 폐기 → 연료 기준 대체 |
| D 측정 불가 | 주행거리 산출 불가 (0km) | 평가 제외 |

**0건에 만점을 주지 않는다.** 이것이 이 판정 체계의 기본 원칙이다.
100km를 넘게 달리고 이벤트가 정확히 0건인 것은 잘 몬 결과가 아니라 계측 실패를 의심할 근거다.

### 1-3. 물리적 상한 — 100km당 100회

1km마다 급가속 또는 급감속이 한 번씩 발생한다는 뜻이다.
총중량 40톤 차량에서 이 빈도는 성립하지 않는다. 이 선을 넘으면 운전 행태가 아니라
단말 임계값 설정 문제로 판단한다.

> **출처 등급: 설정값 (미확정)**
> 차량군 실측 분포의 상위 백분위로 전환 예정.

### 1-4. 위변조 정합성 검사

신뢰등급과 별개로, 제출 데이터의 물리적 일관성을 검사한다.
**고장 탐지와 같은 원리다. 조작을 위한 별도 알고리즘을 두지 않는다.**

| 검사 | 조건 | 의미 |
|---|---|---|
| R2 물리 정합성 | 주행거리 ÷ 운행시간 > 110 km/h | 25톤 트랙터 주행으로 불가능 |
| R3 속도 적분 | Σ(속도 × Δt)와 누적거리 괴리 > ±15% | 주행거리만 별도로 조작된 흔적 |
| R4 시퀀스 중복 | (위도·경도·속도·회전수) 시퀀스 완전 일치 | 구간 복사·붙여넣기 |
| R5 연속성 | 누적 주행거리 역행 또는 비정상 점프 | 값 직접 편집 |

> **출처 등급: 설정값 (미확정)** — 110km/h, ±15% 임계값

**샘플링 간격에 따른 검출력 한계.** R3(속도 적분)과 R4(시퀀스 중복)는 위치 데이터가
조밀할 때만 유효하다. 통합단말기 실데이터는 2분 간격이므로 이 두 검사는 사실상 작동하지 않는다.

> 샘플링 간격이 120초 이상이면 R3·R4는 **「판정 보류」로 표기하고 통과 처리하지 않는다.**
> 검출하지 못한 것을 검출한 것처럼 표시하지 않는다는 원칙을 우리 검사에도 동일하게 적용한다.

---

## 2. 유류사용량 기준 산정

이벤트 데이터를 신뢰할 수 없을 때 사용하는 대체 경로다.
**센서 임계값은 설정으로 바꿀 수 있지만 연료 소모는 바꿀 수 없다**는 점이 근거다.

### 2-1. 원단위

```
연료 원단위 = 연료(L) ÷ 주행거리(km) × 100     →  L/100km
```

### 2-2. 기대 연비 — 3계층 조회

기대 연비는 이 산식 전체의 기준선이다. 그래서 **어디서 가져왔는지를 반드시 함께 표시한다.**
우선순위대로 시도하고, 처음 성공한 것을 쓴다.

| 계층 | 출처 | 신뢰등급 | 적용 |
|---|---|---|---|
| ① | **자동차등록증 ⑫제원란 공인연비** | **A** | 화물차 주 경로 (법정 서류 기재값) |
| ② | **한국에너지공단 자동차 표시연비 (공공데이터포털 API)** | **A** | 승용차 주 경로 (복합연비) |
| ③ | **AI 유사차량 추정** | **C** | 위 둘 다 없을 때 |

**대형 화물차는 ②에 없다.** 에너지소비효율 표시 의무는 총중량 3.5톤 이하 차량 중심이므로
25톤 트랙터는 공공 DB에서 조회되지 않는다. 이는 오류가 아니라 예정된 폴백 경로다.

**③ AI 추정의 조건** — 반드시 참조 모델을 함께 제시해야 하며, 근거 없는 숫자는 반환하지 않는다.
확신이 없으면 신뢰도를 낮춰 표시한다. 낮은 신뢰도가 틀린 숫자보다 낫다.
추정 범위(승용 5~25 km/L, 화물 1.5~12 km/L)를 벗어나면 값을 내지 않는다.

### 2-2-1. 적재상태 보정

조회된 기대 연비는 시험조건 값이므로 실주행 보정계수를 곱한다.

```
기대 연비_공차 = 조회값 × 1.08
기대 연비_적차 = 조회값 × 0.78
```

승용차는 적재상태 구분이 없으므로 보정 없이 조회값을 그대로 쓴다.

**차종별 조회 경로.** 승용차는 ②공공API에서 대부분 조회되고, 대형 트랙터는 ①등록증 또는 ③AI 추정으로 간다.
같은 산식이 서로 다른 출처의 기준선 위에서 동일하게 작동한다는 것이 이 3계층 구조의 목적이다.

> **출처 등급: 설정값 (미확정)** — 보정계수 1.08 / 0.78
> 현재 화물차 데모 기준(등록증 3.6 km/L → 공차 3.90 / 적차 2.80)을 역산해 맞춘 값이다.
> PoC 1차 과제로 차량군 실측 회귀 전환.

### 2-2-2. 장기 목표 — 차량군 중앙값

궁극적으로는 외부 수치가 아니라 자사 실측이 기준선이 되어야 한다.
같은 25톤이라도 노선·구배·정체·적재중량·연식에 따라 연비가 크게 달라지기 때문이다.

```
기대 연비 = 같은 노선 · 같은 적재상태 차량군의 중앙값
```

산출에 필요한 데이터는 두 가지뿐이고, 둘 다 이미 존재한다.

- **유류카드 전표** — 차량번호별 주유일자·주유량(L). 법인카드 사용 차량은 이미 확보 가능
- **운행기록** — 주행거리·적재상태

### 2-3. 연비 손실률

```
연비 손실률 = 1 − (실측 연비 ÷ 기대 연비)
```

### 2-4. 추정 발생률로 환산

```
추정 발생률 = (연비 손실률 ÷ 최대 손실률) × 최대 발생률
```

**전제:** 가장 거친 운전(100km당 30회)이 연비를 12% 악화시키고,
가장 부드러운 운전(0회)은 손실이 없다. 그 사이를 선형으로 본다.

> **출처 등급: 문헌 범위 + 설정값**
> 에코드라이빙 연구에서 운전자 간 연비 편차는 5~15%로 보고된다.
> 12%는 그 범위 내에서 설정한 값이며, 자사 실측 회귀로 확정 예정.

### 2-5. 산출 예시 — 8호차

```
적차 구간 기대 연비                    2.80 km/L
8호차 실측 연비                        2.53 km/L
────────────────────────────────────────────────
연비 손실률   1 − 2.53 ÷ 2.80      =    9.6 %
손실 비율     9.6 ÷ 12             =    0.80
추정 발생률   0.80 × 30            =  100km당 24회
────────────────────────────────────────────────
단말 보고 발생률                      100km당  0회
두 신호의 비율                              × 0.00
```

**이 모순이 판정 근거다.** 연료는 거친 운전을 시사하는데 단말은 완벽하다고 보고한다.

### 2-6. 계수 불확실성이 판정에 미치는 영향

최대 손실률(12%)이 실제와 다르더라도 **모든 차량의 추정치가 같은 배율로 이동**한다.
따라서 이상 차량의 **순위와 검출 순서는 유지되고**, 흔들리는 것은 등급 경계선이다.

- **8호차 같은 사례는 계수와 무관하게 검출된다** — 이벤트가 0이면 비율도 0이다
- **계수에 민감한 것은 경계선에 걸친 B(센서 둔감) 등급이다**
- 그래서 **온보딩 단계에서는 B 등급을 판정하지 않는다.** 연료 데이터가 충분히 축적된 뒤 활성화한다

---

## 3. 배출량 산정

### 3-1. 기본식

```
연료(L)   = 구간거리 ÷ 적재상태별 연비 + 공회전시간 × 시간당 소모량
CO₂(kg)   = 연료(L) × 2.606
원단위     = CO₂(g) ÷ 톤킬로
```

> 경유 CO₂ 배출계수 2.606 kg/L — **출처 등급: 설정값**
> 「온실가스 배출계수 고시」 확정값으로 교체 예정.
> 공회전 시간당 소모량 2.4 L/h — **출처 등급: 설정값**

### 3-1-1. 차종별 원단위 — 합산하지 않는다

| 차종 | 배출 분류 | 원단위 |
|---|---|---|
| 트랙터 (자가 운송) | Scope 1 | gCO₂/ton-km |
| 트랙터 (협력사 위탁) | Scope 3 Cat.4 | gCO₂/ton-km |
| 업무용 승용차 | Scope 1 | **gCO₂/km** |

승용차는 화물을 싣지 않으므로 톤킬로가 성립하지 않는다. 차량킬로 기준으로 별도 산정하며,
**Scope 1과 Scope 3을 한 숫자로 더하지 않는다.** 총계는 Scope별로 분리해 표기한다.

### 3-2. 공차 구간 처리

수출 컨테이너 라운드는 구조가 다음과 같다.

```
부산신항 ──[공컨 견인]──▶ 화주 공장     적재상태 = 공차
화주 공장 ──[  만적  ]──▶ 부산신항     적재상태 = 적차
```

**전체 주행의 정확히 절반이 공차 구간이다.**

- 표준 배출원단위는 평균 적재율을 전제하므로, 이 노선에서는 구조적으로 어긋난다
- 공차 구간은 해당 수출 부킹이 유발한 주행이므로 화주 배출량에 귀속되어야 하나,
  실무에서는 적재 구간만 집계하는 경우가 있어 **현재 보고치가 과소 산정되었을 가능성**이 있다

### 3-3. 기본계수 대비

```
기본계수 산정 = 톤킬로 × 표준 배출원단위 (62.0 gCO₂/ton-km)
실측 산정     = 실제 연료 × 배출계수
```

**두 값의 차이는 감축량이 아니라 계측 오차다.** 감축으로 표기하지 않는다.

---

## 4. 화주 귀속 산정

단말은 차량에 부착되지만, 화주에게 전달되는 단위는 **운행 건**이다.

```
단말 원본 (하루치)
  06:40 ~ 13:20   부산신항 → 경주 → 부산신항     글로비스 건
  14:30 ~ 18:10   경주 → 울산                    타 화주 건
        ↓ 배차 오더와 시간축으로 조인
전달 대상
  06:40 ~ 13:20 구간의 궤적 · 이벤트 · 연료만
```

**비율을 쓰지 않는다.** 건별로 귀속시키면 분모(운송사 전체 물량)가 필요 없으므로,
영업비밀을 노출하지 않고도 화주가 필요한 수치를 전부 제공할 수 있다.

### 구간 귀속 검증 — 지오펜스 단독

신고 구간 자체도 자기신고이므로 검증 대상이다.
**지오펜스 하나만 쓴다.** 터미널 게이트 기록은 미연동이고, 배차 시각은 자기신고라
대조 근거로서 독립적이지 않기 때문이다. 근거를 늘리는 것보다 근거 하나를 정확히 쓰는 편이 낫다.

```
설정 화면에 등록한 사업장 좌표 · 반경 1km
   ↓
GPS 궤적이 원에 진입 / 이탈한 시각을 계산
   ↓
출발 사업장 이탈 시각 ~ 도착 사업장 진입 시각 = 귀속 구간
```

| 판정 | 조건 |
|---|---|
| verified | 출발·도착 사업장 모두 교차 검출 |
| partial | 한쪽만 검출 |
| failed | 둘 다 미검출 |

**판정 방식 — 선분 교차.** 점이 원 안에 있는지로 판정하면 안 된다.
샘플링 간격이 2분이므로 시속 80km에서 2.7km를 이동하며, 반경 1km 지오펜스를 통째로 건너뛴다.
연속한 두 위치 점을 잇는 **선분이 원과 교차하는지**로 판정하고, 교점까지 선형 보간해
진입·이탈 시각을 추정한다.

> 진입·이탈 시각은 추정값이므로 **± (샘플링 간격 ÷ 2)** 의 오차를 함께 표기한다.
> **출처 등급: 계산값 (오차 명시)**

---

## 5. 지표별 출처 등급

**모든 지표에 근거와 신뢰등급을 붙인다.** 자기신고를 배제하지 않되, 자기신고라고 표시한다.

| 지표 | 근거 | 신뢰등급 |
|---|---|---|
| 위험운전 발생률 | 단말 센서 실측 | A |
| 기대 연비 — 등록증 경로 | 자동차등록증 기재 공인연비 | A |
| 기대 연비 — 공공API 경로 | 한국에너지공단 표시연비 | A |
| **기대 연비 — AI 추정 경로** | **유사 차량 추론** | **C** |
| 주행거리 · 적재상태 | 단말 자동 기록 | A |
| 연료 사용량 | 법인 유류카드 전표 (회계 기록) | A |
| 단말 관리 현황 | 시스템 자동 집계 | A |
| 구간 귀속 | 지오펜스 + 반출입 기록 대조 | A |
| 안전운임 지급액 | 국토부 고시 기점별 운임표 | A |
| **정비 점검 이행** | **기사 자기신고** | **D** |

> 정비 이행은 현재 검증 수단이 없다. 정비 전표 또는 지정 정비소 데이터를 연동하면 A로 승격된다.

---

## 6. 미확정 수치 목록

아래는 현재 설정값이며, 자사 실측으로 확정해야 한다.
**6개월 PoC의 과제 목록이기도 하다.**

| 수치 | 현재값 | 확정 방법 |
|---|---|---|
| 적재상태 보정계수 (공차/적차) | ×1.08 / ×0.78 | 노선·적재상태별 차량군 실측 회귀 |
| 기대 연비 조회 실패 시 대체값 | AI 유사차량 추정 (신뢰등급 C) | 자사 차량군 중앙값으로 대체 |
| 최대 연비 손실률 | 12% | 이벤트 발생률 대비 연비 회귀분석 |
| 최대 발생률 기준 | 100km당 30회 | 차량군 실측 분포 상위 백분위 |
| 물리적 상한 | 100km당 100회 | 동일 |
| 평균속도 상한 (R2) | 110 km/h | 실측 운행 분포 |
| 속도적분 허용 오차 (R3) | ±15% | 정상 차량군의 오차 분포 |
| 경유 CO₂ 배출계수 | 2.606 kg/L | 온실가스 배출계수 고시 |
| 공회전 연료 소모 | 2.4 L/h | 공회전 구간 실측 |

### PoC 설계

- **대상** — 세방 자차 트랙터 13대 + 업무용 승용차 10대 (총 23대)
- **기간** — 6개월
- **입력** — 법인 유류카드 전표 + 통합단말기 운행기록
- **산출** — 위 8개 수치의 자사 확정값
- **추가 투자** — 없음. 두 데이터 모두 이미 존재한다
