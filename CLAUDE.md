# CLAUDE.md — S&E Driving Platform 작업 규칙 **v2**

> 이 파일은 **브레이크**다. 무엇을 만들지는 `docs/PRD.md`에 있고, 여기는 **무엇을 만들면 안 되는지**와 매 작업마다 지켜야 할 규칙만 있다.
>
> **GPT·Codex 등 다른 도구를 쓰는 팀원은 이 파일을 자동으로 읽지 않는다.** 매 프롬프트 앞에 §1과 §2를 직접 붙여라.

---

## 0. 이 프로젝트가 무엇인가 (한 문장)

> **이 플랫폼은 운송을 더 잘하게 만들지 않는다. 운송이 어땠는지 증명한다.**

기능 하나를 넣을지 고민되면 이 문장에 걸어라. 걸리면 만들지 않는다.

**핵심 원칙 — 0건에 만점을 주지 않는다.**
100km를 넘게 달리고 위험운전 이벤트가 정확히 0건인 것은 잘 몬 결과가 아니라 계측 실패다. 이 하나가 프로젝트 전체의 논지다.

---

## 1. 절대 만들지 않는 것

| 금지 | 이유 |
|---|---|
| **상단 KPI 카드 4개 + 하단 차트 나열** | 1차 실패의 형태. 이 배치가 나오면 잘못 가고 있는 것 |
| **상단 메뉴바 · 홈 화면 박스 내비게이션 · 빵조각** | v2 신규. 진입점은 좌측 메뉴 하나뿐 |
| **화주뷰 · 협력사뷰 분리** | v2에서 회사뷰로 통일. 역할은 회사/기사 2단뿐 |
| 실시간 차량 위치 관제 화면 | 통합단말기 상용 서비스가 이미 파는 기능 |
| 배차 최적화 · 복화 매칭 · 경로 추천 | 심사위원(글로비스)의 본업을 건드림 |
| 사고 예측 · ML 스코어링 · 확률 수치 | 짧은 기간에 검증 불가. 근거 없는 숫자는 역효과 |
| 기사 실명 노출 · 인사평가 연계 UI | 노무 리스크. 차량ID/기사코드까지만 |
| 실제 권한 관리 화면(계정 생성·역할 부여) | 백엔드 없는 이 프로젝트 범위 밖 |
| 새 판정 등급 · 새 점수 체계 | 화면이 늘어나도 기존 등급(정상/A/B/C/D)·점수(scoreOf)를 **재배열**만 한다 |
| **더미·합성 차량 생성** | v2 신규. 실측 23대(트랙터 13 + 승용 10)뿐이다. `fleet.json`·`fleetGen.mjs`·`fleetDriverDemo.ts`·`syntheticMaintStatus` 전부 만들지 않는다 |
| **Scope 1과 Scope 3 합산** | 온실가스 회계 오류. 트랙터와 승용차 배출량을 한 총계에 넣지 않는다 |

### 1-1. 증명서에 절대 넣지 않는 것 (v2 신규)

증명서는 **대외 제출 문서**다. 대내 관리 지표를 섞으면 문서가 아니라 대시보드가 된다.

- ❌ S&E 점수 · 운전 점수 · 우수 운전자 배지
- ❌ 기사 코드 · 기사 관련 일체
- ❌ 정비 자기신고 상태
- ❌ 연료 절대량 · 유류비

**증명서는 사실만 담고 판단은 담지 않는다.**

---

## 2. 매 작업 체크리스트

작업을 끝내기 전에 매번 확인한다.

1. **하드코딩 hex를 넣지 않았는가** — 컴포넌트에서 `grep -E '#[0-9a-fA-F]{3,6}'` 결과가 0이어야 한다. 색은 `index.css` 토큰과 `.tone-*` 클래스로만.
2. **등급→톤 매핑을 새로 쓰지 않았는가** — `GRADE_META` 한 곳에서만 나온다.
3. **`_truth.csv`를 읽지 않았는가** — 정답표다. 앱·번들 어디서도 로드 금지. `lib/csv.mjs`가 파일명 정규식으로 차단한다.
4. **없는 데이터를 있는 척하지 않았는가** — 시뮬레이션·자기신고·AI 추정은 **화면에 배지로 고정 표기**한다.
5. **숫자에 출처가 붙어 있는가** — 특히 기준연비는 `등록증 / 공공API / AI추정` 배지가 반드시 따라다닌다.
6. **네트워크가 죽어도 화면이 사는가** — §4 참조.

---

## 3. 파일 소유권 (충돌 방지)

**각자 자기 파일만 건드린다.** 공유 파일은 오전에 A가 확정하고 **이후 수정 금지** — 바꿔야 하면 A에게 말하고 A만 고친다.

```
공유(A만 수정)   src/types.ts · src/index.css · src/App.tsx · src/components/Sidebar.tsx
                scripts/lib/constants.mjs · scripts/lib/csv.mjs
```

| 담당 | 파일 |
|---|---|
| A | `SafeScreen.tsx` + 공유 파일 전체 |
| B | `CertificateScreen.tsx` · `Certificate.tsx` · `SettingsScreen.tsx` |
| C | `IngestScreen.tsx` · `lib/ingest/` · `lib/baselineFuel/` |
| D | `EcoScreen.tsx` · `HeatmapScreen.tsx` · `CorridorMap.tsx` |
| E | `scripts/` 전체 · `DriverScreen.tsx` · 배포 |

---

## 4. 네트워크 규칙

**네트워크가 필요한 곳은 3곳뿐이고, 셋 다 폴백이 있어야 한다.**

| 용도 | 실패 시 |
|---|---|
| LLM 스키마 매핑 (Gemini) | 수동 드롭다운 매핑으로 진행. "결과" 버튼은 **파싱이 시작만 되면** 항상 활성 |
| 기준연비 조회 (공공데이터포털) | `public/fixtures/fuel_economy_cache.json` 읽기 |
| 지도 (카카오맵) | 오프라인 SVG `CorridorMap`으로 자동 대체 (6초 타임아웃) |

**기준연비는 빌드 타임에 미리 조회해 `baseline_fuel.json`으로 굳힌다.** 런타임에 API를 때리지 않는다 — 발표 중 API가 죽으면 Safe 화면 전체가 무너진다.

LLM 호출 공통: `attempts: 1`, `timeout: 30000`. SDK 기본 재시도는 **끈다**(503 시 총 대기가 1분을 넘어 "멈춘 것"처럼 보임). 8초 넘으면 "오래 걸리는 중" 안내 + 다음 단계 버튼은 계속 활성.

---

## 5. 데이터 정합성 규칙

### 5-1. 순위는 필터와 무관하다

순위는 **항상 전체 기준**으로 계산한 뒤, 필터는 "보여줄 행"만 고른다. 필터를 걸었다고 1등이 바뀌면 안 된다.

### 5-2. D등급은 순위에서 뺀다

주행거리 0 = 정규화 분모 없음. 순위 사다리가 아니라 **"평가 제외" 섹션**에 따로 둔다.

### 5-3. 지오펜스는 선분 교차 판정이다 ★

실데이터 샘플링이 2분 간격이다. 시속 80km면 2분에 2.7km를 이동하므로 반경 1km 원을 **통째로 건너뛴다.**

```
❌ dist(point, center) < R
✅ segment(p_i, p_i+1) 이 원과 교차하는가 → 교점까지 선형 보간해 시각 추정
```

점 포함 판정으로 구현하면 증명서 구간검증이 전부 failed로 나온다.

### 5-4. 못 잡는 건 잡은 척하지 않는다

샘플링 간격이 120초 이상이면 R3(속도적분)·R4(시퀀스중복)는 검출력이 없다. **"판정 보류"로 표기**하고 통과 처리하지 않는다.

### 5-5. 차종 분기는 딱 3곳뿐이다

트랙터도 승용차도 통합단말기가 달려 있다. 이벤트 축이 양쪽 다 살아 있으므로 **등급 판정 로직은 차종을 구분하지 않는다.** 새 등급을 만들지 마라.

갈리는 것은 이 3가지뿐이다.
1. 기준연비 조회 순서 — `car`는 공공API 먼저, `truck`은 등록증 먼저
2. 적재상태 보정 — `car`는 보정계수를 곱하지 않음
3. 증명서 발급 대상 — `car`는 제외(화물 운송을 하지 않음)

### 5-6. 표기 금지어

- 기본계수 대비 실측의 차이 → **"감축량" 아님. "계측 오차"**
- 기준연비 초과분 → **"감축량" 아님. "감축 여지"(상한)**
- Tier와 신뢰등급 → **절대 합산 금지.** 별도 축으로 나란히만 표시
- 승용차 배출 원단위 → gCO₂/ton-km 아님. **gCO₂/km** (톤킬로 없음)

---

## 6. 커밋 규칙

- **대회 당일 레포를 새로 만들고, 모든 커밋을 당일에 한다.**
- 첫 커밋에 README로 사전 자산을 먼저 밝힌다:
  > 데이터 픽스처(`files2/`)와 설계 문서(`docs/`)는 사전 준비한 자산입니다.
  > 애플리케이션 코드는 전량 대회 당일 작성했습니다.
- **팀원 각자 자기 이름으로 커밋한다.** 한 사람이 몰아서 올리지 않는다.
- `.env.local`은 `.gitignore`에 **먼저** 넣는다. API 키가 커밋에 들어가면 그 자리에서 폐기해야 한다.
- 유비칸 원본 파일은 로컬 전용. **커밋 금지.**
- **실측 차량번호는 전부 익명화한다.** 배포본·데모 영상 모두 `SB-0000xx` 네임스페이스로 치환.
- 데모 영상용으로 차량번호는 익명화한다.

---

## 7. 오늘의 마감선

| 시각 | |
|---|---|
| 13:00 | 중간 점검 — **Safe 화면이 도는가?** 안 되면 전원 투입 |
| **16:00** | **개발 마감선.** 여기서 안 된 건 버린다 |
| 16:00–17:00 | 통합 · 버그 · 배포 |
| 17:00–18:00 | 데모 영상 촬영 + 유튜브 업로드 |
| 18:00–19:00 | 결과보고서 · 리허설 |

**16시가 진짜 마감이다.** 배포·영상·보고서에 3시간이 필요하다.

**자르는 순서** — `13 → 12 → 11 → 10 → 9 → 7 → 6 → 5` (BUILD_SEQUENCE 트랙 번호)
**0·1·2·3·4는 절대 자르지 않는다.**
