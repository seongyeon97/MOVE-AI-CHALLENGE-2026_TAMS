# S&E Driving Platform — 재구현 설계서 (PRD) **v2**

> **v2 개정 (2026-08-13)** — 어제 최종 시뮬레이션에서 제3자 평가가 "어렵다"로 수렴했다. 플랫폼의 의도와 판정 논리는 그대로 두고, **정보 구조와 진입 동선만** 재설계한다. 판정 수식(§5)은 v1에서 한 줄도 바꾸지 않았다.
>
> **이 문서가 유일한 사양서다.** v1 PRD는 폐기한다. 코드와 이 문서가 어긋나면 이 문서를 따른다.

---

## 0. v1 → v2 변경 요약

**이것만 읽고 시작해도 되게 쓴다.**

| # | 변경 | v1 | v2 |
|---|---|---|---|
| 1 | 뷰 구조 | 화주뷰 / 협력사뷰 / 기사뷰 (3단) | **회사뷰 / 기사뷰 (2단)** |
| 2 | 상단 메뉴바 | 있음 (역할 토글·빵조각) | **삭제** |
| 3 | 좌측 메뉴 | 진단·검증·증명서 + 부가도구 산재 | **Safe / Eco / Heat-map / 증명서 발급 / 설정 (5개 고정)** |
| 4 | 진단·검증 | 별도 화면 2개 | **Safe 한 화면으로 통합** |
| 5 | 증명서 | 클릭 경로의 종착점 | **좌측 메뉴 독립 항목** |
| 6 | S&E 점수 | 증명서 히어로에 대문짝 | **차량 관리 목적 전용. 증명서에 미표기** |
| 7 | 대상 차종 | 25톤 트랙터 전제 | **화물차·승용차 무관. 운행정보만 있으면 동작** |
| 8 | 기준 연비 | 코드 상수 (3.9 / 2.8 km/L) | **공공데이터포털 API 연동 + AI 유사차량 추정 (§4)** |
| 9 | 구간 검증 | 지오펜스 + 배차 + 터미널 3중 대조 | **지오펜스 단독. 설정에서 사업장 등록 → 반경 1km 자동 생성** |
| 10 | 구간분석 | 별도 부가도구 화면 | **Heat-map 메뉴로 승격 (카카오맵)** |
| 11 | 차량 규모 | 실측 10대 + 더미 212대 + 화주 99대 | **실측 23대 (트랙터 13 + 승용 10). 더미 전면 삭제** |

**삭제되는 것** — 화주뷰 전체(`glovis_fleet.json`, `glovis_fleet_summary.json`, `CarrierBoardScreen`(v1 화면5), `hideCost` 분기), 상단 메뉴바, 증명서의 `VerdictHero` 점수·기사코드·배지, `attribution.json`의 터미널/배차 대조 축, **더미 차량 생성 일체**(`fleet.json`, `build-fleet.mjs`, `fleetGen.mjs`, `fleetDriverDemo.ts`, `syntheticMaintStatus`).

**유지되는 것** — 신뢰등급 5단계(정상/A/B/C/D), 연료 교차검증, R2~R5·F1/F2 정합성 검사, LLM 스키마 매핑, 디자인 토큰 전체, 기사뷰.

---

## 1. 개요

### 1.1 한 줄 정의

> **이 플랫폼은 운송을 더 잘하게 만들지 않는다. 운송이 어땠는지 증명한다.**

기능을 추가할지 고민될 때 이 문장에 걸어보고, 걸리면 만들지 않는다. v2에서도 이 기준선은 그대로다.

### 1.2 v2가 푸는 문제 — "어렵다"

제3자 평가의 실패 원인은 기능이 아니라 **구조**였다.

| 증상 | 원인 | v2 처방 |
|---|---|---|
| 역할 토글의 의미를 이해 못 함 | 화주/협력사 두 뷰의 차이가 "무엇을 숨기느냐"뿐이라 눈에 안 보임 | 회사뷰로 통일. 숨김 로직 제거 |
| 진단과 검증의 차이를 설명해야 이해됨 | 같은 표를 두 번 보여주는 구조 | Safe 한 화면에 통합 |
| 어디로 가야 하는지 모름 | 상단 메뉴 + 좌측 메뉴 + 홈 박스, 진입점 3중 | 좌측 메뉴 5개 고정. 나머지 내비 전부 제거 |
| 증명서에 점수·기사·배지가 섞여 문서로 안 보임 | 대내 관리 지표와 대외 제출 문서를 한 장에 넣음 | 증명서는 안전·ECO 사실만. 점수는 Safe에만 |

### 1.3 절대 만들지 않는 것

| 금지 | 이유 |
|---|---|
| 상단 KPI 카드 4개 + 하단 차트 나열 레이아웃 | 1차 실패의 형태. 이 배치가 나오면 잘못 가고 있는 것 |
| 실시간 차량 위치 관제 화면 | 기존 통합단말기 상용 서비스가 이미 파는 기능 |
| 배차 최적화 · 복화 매칭 · 경로 추천 | 심사위원(글로비스)의 본업을 건드리는 것 |
| 사고 예측 · ML 스코어링 | 짧은 개발 기간 안에 검증 불가 |
| 기사 개인 실명 노출 · 인사평가 연계 UI | 노무 리스크. 차량ID/기사코드까지만 |
| 실제 권한 관리 화면 | 백엔드 없는 이 프로젝트 범위 밖 |
| **상단 메뉴바 · 홈 화면 박스 내비게이션** | **v2 신규 금지.** 진입점은 좌측 메뉴 하나뿐 |

### 1.4 핵심 원칙

1. **0건에 만점을 주지 않는다.** 이벤트 0건 + 장거리 주행은 "완벽한 운전"이 아니라 "센서 미작동 의심"이다(§5.2 C등급).
2. **새 판정 체계를 만들지 않는다.** 화면이 늘어나도 전부 기존 등급·점수를 재배열·재사용한다.
3. **없는 데이터를 있는 척하지 않는다.** 시뮬레이션·자기신고·AI 추정은 화면에 고정 표기한다.
4. **모든 숫자에 출처 등급을 붙인다.** 특히 v2의 기준연비는 3계층 출처를 갖는다(§4.3).
5. **대내 지표와 대외 문서를 섞지 않는다.** S&E 점수·기사 배지·정비 자기신고는 Safe/기사뷰에만. 증명서에는 안 들어간다.
6. **오프라인 우선.** 네트워크가 필요한 곳은 3개뿐 — 데이터 업로드(LLM 매핑), 기준연비 API, Heat-map 지도. 셋 다 실패 시 폴백이 있어야 한다.

### 1.5 배경 (요약)

한국 안전운임제(2026-01-01 재도입, 2028-12-31 일몰)는 "적정 운임 → 과로·과속·과적 방지 → 교통안전 확보"라는 논리인데, 마지막 고리를 입증할 데이터가 없다. 세방 광양 트랙터 13대 실측 진단 결과 신뢰 가능 차량이 0대였다. 이 플랫폼은 "단말부터 깔기 전에 검증 레이어를 먼저 가져야 한다"의 증거다.

**v2에서 확장된 대상** — 이 논리는 25톤 트랙터에만 성립하는 게 아니다. 운행정보(위치·속도·주행거리)와 연료 실적이 있으면 어떤 차종이든 같은 검증이 성립한다. 그래서 v2는 화물차/승용차를 구분하지 않는다.

### 1.6 차량 구성 (v2 확정)

**전부 실측 23대다. 더미 차량은 만들지 않는다.**

| 구분 | 대수 | 단말 | 기준연비 경로 | 적재상태 | 증명서 |
|---|---:|---|---|---|---|
| 트랙터 | **13** | 통합단말기 | 등록증 → AI 추정 | 공차/적차 구분 | **발급 대상** |
| 업무용 승용차 | **10** | 통합단말기 | **공공API** → AI 추정 | 구분 없음 | 대상 아님 |

**두 차종 모두 통합단말기가 달려 있다.** 위험운전 이벤트 축이 살아 있으므로 §5.2 신뢰등급 판정이 차종 구분 없이 그대로 적용된다. 별도 등급을 만들지 않는다.

**차종에 따라 갈리는 것은 딱 3가지다.**

1. **기준연비 조회 경로** — 승용차는 공공API에서 나오고, 대형 트랙터는 안 나온다(§4.2). 그래서 한 화면에 `공공API`(teal) / `등록증`(teal) / `AI추정`(amber) 세 배지가 함께 뜬다. **이 화면이 "차종을 안 가린다"의 증거다.**
2. **적재상태 보정** — 승용차는 공차/적차 구분이 없으므로 보정계수를 곱하지 않는다(§4.5).
3. **증명서 발급 대상** — 승용차는 화물 운송을 하지 않으므로 대외 증명서 대상이 아니다. Safe와 Eco 화면에만 나온다.

> ⚠️ **트랙터 13대의 출처를 확정할 것** — 어제 진단한 광양 실측 13대인지, 글로비스 합성 데이터를 13대로 줄인 것인지에 따라 `files2/` 원천 CSV가 달라진다. 실측이면 차량번호 익명화가 필수다.

---

## 2. 정보 구조

### 2.1 역할 (2단)

| 역할 | 화면 | 대상 |
|---|---|---|
| **회사** | 좌측 메뉴 5개 전체 | 운송사(세방) 관리자 |
| **기사** | 단일 화면(폰 목업) | 차량 기사 |

로그인 시 역할을 고르고, 로그인 후 전환은 **좌측 메뉴 최하단의 작은 전환 링크** 하나로만 한다(발표 중 전환용). 우상단 토글은 없앤다 — 상단 메뉴바가 사라졌기 때문.

⚠️ **확인 필요** — 회사뷰로 통일하면서 "누가 누구를 검증하는가"의 화자가 바뀐다. v1은 *화주(글로비스)가 협력사를 본다*였고, v2는 *우리(세방)가 우리 차량을 관리하고, 그 결과를 원청(글로비스)에 증명서로 제출한다*가 된다. **심사위원이 글로비스라는 점에서 v2 쪽이 더 자연스럽다** — 발표 서사도 이 방향으로 맞추는 걸 전제하고 썼다.

### 2.2 좌측 메뉴 (회사뷰, 고정 5개)

```
┌─────────────────┐
│  S&E Driving    │
│                 │
│  ◆ Safe         │  안전운전 진단·검증 + 차량 관리
│  ◆ Eco          │  연비 기반 탄소 관리 (Scope 3)
│  ◆ Heat-map     │  구간 위험도 (카카오맵)
│  ◆ 증명서 발급   │  대외 제출 문서
│  ─────────────  │
│  ⚙ 설정         │  사업장·운송구간·지오펜스
│  ─────────────  │
│  ↔ 기사뷰로      │
└─────────────────┘
```

- 상단 메뉴바 없음. 빵조각 없음. 홈 화면 없음 — **로그인 직후 바로 Safe로 진입한다.**
- 데이터 업로드(LLM 매핑)와 조작탐지 데모는 메뉴 항목이 아니라 **Safe 화면 상단의 액션 버튼**으로 들어간다(§3.1).
- 미확인 공지·단말점검 요청이 있으면 Safe 항목 옆에 점 하나.

### 2.3 화면 목록

| 화면 | 컴포넌트 | v1 대응 |
|---|---|---|
| 로그인·회원가입 | `LoginScreen.tsx` | 유지 (역할 3→2단) |
| **Safe** | `SafeScreen.tsx` | v1 §2.3 진단 + §2.4 검증 + §2.9 차량대시보드 통합 |
| **Eco** | `EcoScreen.tsx` | 신규 (v1 배출량 로직 재사용) |
| **Heat-map** | `HeatmapScreen.tsx` | v1 §2.8 구간분석 승격 |
| **증명서 발급** | `CertificateScreen.tsx` | v1 §2.5 (점수·기사 제거) |
| **설정** | `SettingsScreen.tsx` | 신규 |
| 데이터 업로드 | `IngestScreen.tsx` | 유지 (Safe에서 진입) |
| 조작탐지 데모 | `IntegrityCompareScreen.tsx` | 유지 (업로드 결과에서 진입) |
| 기사뷰 | `DriverScreen.tsx` | 유지 (변경 없음) |

---

## 3. 화면별 상세

### 3.1 Safe — 안전운전 진단·검증·관리

**한 화면에서 끝난다.** v1의 진단→검증 2단 동선이 "어렵다"의 가장 큰 원인이었다. 같은 차량 목록을 두 번 보여주면서 컬럼만 달랐기 때문이다.

#### 화면 상단 — 판정 요약 한 줄

```
23대 중 9대 데이터 신뢰 불가 · 검증 커버리지 61%
```

이 한 줄이 헤드라인이다. 카드 4장으로 쪼개지 않는다.

우측에 액션 버튼 3개: `데이터 업로드` / `리포트 생성` / `전체 공지 발송`.

#### 본문 — 단일 차량 표

**컬럼 (좌→우)**

| 컬럼 | 내용 | 출처 |
|---|---|---|
| 순위 | `fuel_implied_rate` 기준 전체 순위 | §5.3 |
| 차량 | vehicle_id + 차종 배지(화물/승용) | |
| 단말 | 단말모델 + 신뢰등급 배지 | §5.2 |
| 주행거리 | km | 통합단말기 |
| 이벤트 | 급가속/급출발/급감속/급정지 합계 | 통합단말기 |
| 발생률 | 건/km | §5.1 |
| **기준연비** | km/L + **출처 배지**(등록증/공공API/AI추정) | **§4 신규** |
| 실측연비 | km/L | 유류사용량 |
| 5개월 추이 | 순위 스트립 5칸(등급 톤 배경, 최신월만 불투명) | |
| S&E 점수 | 0~70, 산출 불가면 `—` | §5.9 |
| 상태 | 공지 확인 · 단말점검 요청 (점 2개) | |

**필터** — 차종(전체/화물/승용) · 신뢰등급 · 차량ID 검색. 필터를 걸어도 **순위 숫자는 안 바뀐다**(항상 전체 기준으로 계산 후 보여줄 행만 고름). 이건 v1에서 지킨 규칙이고 v2에서도 지킨다.

**D등급 분리** — "평가 제외" 섹션으로 표 아래에 따로 둔다("주행거리 0 — 정규화 분모 없음").

**펀치라인 배너** — 4월 대비 8월 순위 변동이 가장 큰 차량 1대 하이라이트. v1의 데모 하이라이트(관측 1위 → 연료검증 8위 반전)가 여기 산다. **이 장면은 절대 자르지 않는다.**

#### 행 클릭 → 상세 패널 (그 자리 아래로 펼침)

한 행 안에서 v1의 진단·검증·리포트가 전부 나온다.

1. **판정** — 등급 배지 + 한 줄 사유 + 판정 근거 3줄(관측 발생률 / 연료가 시사하는 발생률 / 두 신호 비율)
2. **이벤트 분해** — 4대 유형 건수 막대
3. **연비 교차검증** — 기준연비(출처 배지 포함) vs 실측연비 vs 초과율
4. **리포트** — `buildVehicleReport` 결과 문장(§5.8)
5. **액션** — `공지사항 발송` 버튼

#### Safe에 들어오는 것 / 안 들어오는 것

들어옴: 위험운전 이벤트 모니터링, 신뢰등급 판정, 연료 교차검증 순위, 차량별 리포트, 공지 발송·읽음확인, 단말기 이상 확인, S&E 점수.

안 들어옴: 탄소·배출량 일체(→ Eco), 지도(→ Heat-map), 대외 문서(→ 증명서).

---

### 3.2 Eco — 연비 기반 탄소 관리 (Scope 3)

⚠️ **상세 내용 미확정** — 사용자가 별도로 설명 예정. 아래는 v1 §4.5 로직을 그대로 옮긴 **골격만**이다. 구조를 잡아두고 내용은 나중에 채운다.

#### 골격

1. **상단 요약** — 총 배출량(tCO₂e) · 1차 데이터 비율(%) · Tier 분포
2. **차량별 표** — 차량 / 차종 / **Scope** / Tier / 기준연비(출처) / 실측연비 / 연료(L) / CO₂(kg) / 원단위 / **감축 여지(kg)**
3. **기본계수 대비** — 실측 산정 vs 표준 배출원단위(62.0 gCO₂/ton-km) 비교
4. **공차 구간 비중** — 이 노선은 주행의 절반이 공차라 표준원단위 방식이 구조적으로 틀린다

#### 절대 지킬 표기 규칙 (v1에서 승계)

- **기본계수 대비 실측의 차이는 "감축량"이 아니라 "계측 오차"다.** 감축으로 표기 금지.
- **"감축 여지"는 실적이 아니라 상한이다.** 기준연비 초과분을 없앴을 때의 최대치일 뿐.
- **Tier와 신뢰등급은 절대 합산하지 않는 별도 축이다.** Tier 3(실측 연료 있음)이면서 단말이 D(측정불가)일 수 있다. 나란히 표시만 한다.

#### ⚠️ Scope 구분 — 트랙터와 승용차는 같은 칸에 합산하면 안 된다

| 차종 | 배출 분류 | 원단위 |
|---|---|---|
| 트랙터 (자가 운송) | **Scope 1** — 직접 배출 | gCO₂/ton-km |
| 트랙터 (협력사 위탁) | **Scope 3 Cat.4** — 업스트림 운송 | gCO₂/ton-km |
| 업무용 승용차 | **Scope 1** — 직접 배출 | **gCO₂/km** (톤킬로 산정 불가) |

**승용차는 화물을 싣지 않으므로 톤킬로가 없다.** 원단위를 gCO₂/ton-km로 계산하면 분모가 0이거나 무의미하다. 승용차 행은 **차량킬로 기준(gCO₂/km)** 으로 별도 표기하고, 총계에서 트랙터와 합산하지 않는다.

Scope 1과 Scope 3을 한 숫자로 더하는 것은 온실가스 회계에서 명백한 오류다. 화면 상단 총계는 **Scope별로 분리**해서 낸다.

---

### 3.3 Heat-map — 구간 위험도

카카오맵 위에 위험운전 이벤트를 표시한다. **카카오맵 연동은 다른 TF 팀원 담당** — 이 PRD는 그 사람이 끼워 넣을 인터페이스와, 지도 없이도 도는 폴백까지만 정의한다.

#### 핵심 난제 — 경로가 없다

통합단말기 데이터에는 주행 경로(폴리라인)가 없다. **2분 간격 위치 점**만 있다. 그래서 경로는 추정해야 한다.

```
2분 간격 위치 점 → [AI 경로 추정] → 추정 폴리라인 → 이벤트 좌표 배정 → 구간 위험도
                                                              ↓
                                            [AI 위험요소 평가·조언]
```

#### AI가 하는 일 두 가지 (역할을 섞지 말 것)

**① 경로 추정** — 연속한 두 위치 점 사이를 잇는다. 2분이면 고속도로에서 2~3km가 비어 있다.
- 1차: 결정적 보간(두 점 사이 직선 + 도로 스텁 각도). 네트워크 불필요.
- 2차(가능하면): 카카오 길찾기 API로 두 점 사이 실제 경로 조회 → 폴리라인 대체.
- **추정이라는 사실을 지도 범례에 고정 표기한다.** 실측 궤적처럼 보이게 하지 않는다.

**② 위험요소 평가·조언** — 이벤트가 몰린 구간에 대해 LLM이 한 문단:
- 입력: 구간 좌표, 이벤트 유형 분포, 발생 건수, 통행 건수, (있으면) 도로 유형
- 출력: 위험 요인 추정 1~2문장 + 운전 조언 1문장
- **금지** — 사고 예측, 확률 수치, 근거 없는 원인 단정. "~로 보인다 / ~일 가능성"의 어법만.

#### 집계 규칙 (v1 §4.7 그대로)

```
1. 노선별 대표 폴리라인을 공간 기준선으로
2. 기준선을 8등분
3. verifiable(신뢰등급 정상) 차량 이벤트만 최근접 구간에 배정
4. 구간 발생률 = 구간 이벤트수 ÷ 그 노선 verifiable 운행 건수
5. 내림차순 → 상위 3개 '위험'(rose), 다음 5개 '주의'(amber), 나머지 '양호'
```

**신뢰등급 필터가 핵심이다.** 필터 없이 집계하면 핫스팟이 아니라 "센서 이상 차량이 많이 지나간 자리"가 위험구간으로 나온다.

#### 레이아웃

`xl:grid-cols-[1fr_360px]` — 좌측 지도, 우측 구간 랭킹 리스트. 구간 클릭 시 우측에 AI 평가 문단 표시.

#### 폴백 (필수)

카카오 키 없음 / 로드 실패(6초 타임아웃) → 기존 `CorridorMap`(오프라인 SVG)로 자동 대체. **발표장 네트워크가 죽어도 이 화면은 살아 있어야 한다.**

---

### 3.4 증명서 발급 — 대외 제출 문서

**용도가 v1과 달라졌다.** v1은 화주가 정산을 승인하는 화면이었다. v2는 **우리가 원청·납품처에 제출하는 문서**다.

> 우리는 이 운송을 안전하게, 그리고 친환경적으로 수행했다 — 그리고 그 데이터는 검증됐다.

#### 문서 구성 (위→아래)

1. **헤더** — 문서유형 / trip_id / 오더번호 / `{origin} → {destination}` / 일시 / 규격·컨테이너 / 운송사 / 차량 / 단말 / 수신처
2. **구간 귀속 검증** ← **이 문서의 존재 이유. 최상단에 둔다.**
   - 차량의 그날 운행이 전부 이 회사 물량은 아니다. 어느 구간이 귀사 건인지 확정한다.
   - 지오펜스 기준 출발/도착 판정 결과 + 편차(분) + 판정(verified/failed)
   - 설정(§3.5)에서 등록한 사업장 기준
3. **안전(Safe)** — 위험운전 이벤트 4종 건수 / 연속운전(최대 블록·한도·준수여부) / **데이터 신뢰등급 배지**
   - `settle === 'block'`이면 이벤트에 취소선 + "이행검증 불가" 표기
4. **친환경(Eco)** — CO₂ 실측(kg) / 원단위(gCO₂/ton-km) / 기본계수 대비(%) / Tier / 공차 구간 비중
5. **데이터 신뢰 고지** — 각 수치의 출처 등급표(§5.5)
6. **하단 액션** — `증명서 발행` 토글 / `PDF 내려받기`

#### 증명서에 절대 넣지 않는 것 (v2 신규 제약)

- ❌ S&E 점수 · 운전 점수 · 우수 운전자 배지
- ❌ 기사 코드 · 기사 관련 일체
- ❌ 정비 자기신고 상태
- ❌ 연료 절대량 · 유류비 (원가 노출)

이유: 대외 문서에 기사 개인 평가가 들어가면 노무 리스크이고, 원청이 "이 기사 빼달라"고 요구할 근거가 된다. 그리고 점수는 우리가 만든 척도라 대외 문서의 신뢰도를 오히려 깎는다. **증명서는 사실만 담고, 판단은 담지 않는다.**

#### 대상 차량

**트랙터 13대만.** 승용차는 화물 운송을 하지 않으므로 증명서 발급 대상이 아니다. 선택기 목록에 아예 나오지 않는다.

#### 선택기

운송구간(설정에서 등록한 것) → 기간 → 신뢰등급, 3단 종속 드롭다운. 앞 필터가 바뀌면 뒤는 `'all'`로 리셋.

**대비 보기 토글** — 유지. 같은 노선·같은 규격의 `검증됨 ↔ 검증불가` 한 쌍을 나란히(`xl:grid-cols-2`). 이 장치가 "검증한다"의 의미를 말 없이 설명한다.

---

### 3.5 설정 — 사업장 · 운송구간 · 지오펜스

v2 신규 화면. **증명서의 구간 검증이 여기 의존한다.**

#### 데이터 모델

```ts
type Site = {
  site_id: string;
  name: string;          // "부산신항 T3", "경주공장"
  address: string;       // 도로명 주소
  lat: number; lon: number;
  radius_m: number;      // 기본 1000, 사용자 조정 가능
};

type Corridor = {
  corridor_id: string;
  name: string;          // "부산신항 → 경주"
  origin_site_id: string;
  destination_site_id: string;
};
```

`localStorage` 키 `se.settings.v1`에 저장. 백엔드 없음.

#### 화면 구성

**① 사업장 등록**

- `사업장 추가` 버튼 → 모달
- 모달 안: 사업장명 입력 + **주소 검색**
  - 검색 입력창에 키워드 입력 → 후보 목록 → 선택 → 좌표 자동 확정
  - 선택하면 모달 하단 **미니 지도**에 마커 + **반경 1km 원**이 즉시 그려진다
  - 반경 슬라이더(500m ~ 3km, 기본 1km)
- 등록된 사업장은 카드 목록으로. 각 카드에 이름 / 주소 / 좌표 / 반경 / 수정·삭제

**② 운송구간 등록**

- `운송구간 추가` → 출발지 select + 도착지 select (등록된 사업장에서 고름) + 구간명
- **여러 개 등록 가능.** 목록으로 관리.
- 각 구간 카드에 "이 구간 증명서 N건" 카운트 표시

**③ 지오펜스 미리보기**

- 등록된 모든 사업장의 원을 한 지도에 겹쳐 표시
- 원이 겹치는 사업장이 있으면 경고("두 사업장의 지오펜스가 겹칩니다 — 구간 판정이 모호해집니다")

#### 지도 검색 인터페이스 (카카오맵 담당자에게 넘길 계약)

```ts
// 이 두 함수만 구현해 주면 나머지는 붙는다.
searchAddress(keyword: string): Promise<AddressCandidate[]>
// AddressCandidate = { display_name, road_address, lat, lon }

renderMiniMap(el: HTMLElement, opts: {
  center: [lat, lon];
  markers: { lat, lon, label }[];
  circles: { lat, lon, radius_m }[];
}): { destroy(): void }
```

**폴백** — 카카오 키 없으면 검색창 대신 **위경도 직접 입력** 필드로 전환하고, 미니 지도 자리에는 "지도 미연동 — 좌표 직접 입력" 안내를 띄운다. 설정 화면이 지도 때문에 막히면 안 된다.

---

### 3.6 기사뷰 — 변경 없음

v1 §2.6 그대로 유지한다. 트럭 점검 다이어그램, S&E 점수 히어로, 단말점검 요청 버튼, 공지 확인, 배지 — 전부 그대로.

**한 가지만 확인** — 기사뷰의 S&E 점수는 유지된다. "S&E 점수는 차량 관리 목적으로만"이라는 v2 결정은 *증명서에서 뺀다*는 뜻이지, 기사 본인에게 안 보여준다는 뜻이 아니다.

---

## 4. 기준연비 조회 (v2 신규) ★

**v2에서 가장 중요한 신규 기능이다.** v1은 기준연비를 코드 상수(공차 3.9 / 적차 2.8 km/L)로 박아뒀다. 이건 25톤 트랙터에만 통하는 값이고, "설정값(미확정)"으로 표기해야 했던 약점이었다.

v2는 이걸 **차종별로 조회**한다. 그래서 화물차·승용차 구분 없이 동작한다.

### 4.1 연동 대상

**공공데이터포털 — 한국에너지공단 자동차 표시연비 정보**

- 데이터셋: `한국에너지공단_자동차 표시연비` (data.go.kr)
- 제공 항목: 모델명 / 제조사 / 차종 / 유형 / **복합연비** / 도심연비 / 고속도로연비 / 등급
- 약 2,800개 이상 모델
- 인증: `serviceKey` 쿼리 파라미터 (공공데이터포털 발급)

⚠️ **당일 반드시 확인할 것** — 활용신청 승인이 즉시 나는지. 일부 API는 **자동승인이지만 키 활성화까지 1시간** 걸린다. 승인이 늦어지면 §4.4 폴백으로 간다.

### 4.2 결정적 제약 — 대형 화물차는 이 DB에 없다

에너지소비효율 표시 의무는 총중량 3.5톤 이하 차량 중심이다. **25톤 트랙터는 이 API에서 안 나온다.**

이걸 약점으로 두지 말고 **설계의 근거로 쓴다.**

> 승용차는 공공 DB에 기준연비가 있다. 대형 화물차는 없다.
> 그래서 화물차는 자동차등록증 기재 공인연비를 쓰고, 그것도 없으면 AI가 유사 차량에서 추정한다.
> **어느 경로로 얻었는지를 화면에 표시한다.**

이게 이 플랫폼의 일관된 철학이다 — 자기신고를 배제하지 않되, 자기신고라고 표시한다.

### 4.3 기준연비 3계층 (출처 등급)

**우선순위대로 시도하고, 처음 성공한 것을 쓴다.**

| 계층 | 출처 | 신뢰등급 | 배지 색 | 적용 |
|---|---|---|---|---|
| **① 등록증** | 자동차등록증 ⑫제원란 공인연비 | **A** | teal | 화물차 주 경로 (예: 3.6 km/L) |
| **② 공공API** | 에너지공단 표시연비 복합연비 | **A** | teal | 승용차 주 경로 |
| **③ AI 추정** | LLM이 유사 차량에서 추론 | **C** | amber | 둘 다 없을 때 |

```
lookupBaselineFuel(vehicle) {
  if (vehicle.registered_kmpl) return { value, source: 'registration', trust: 'A' };

  const hit = await publicApi.search(vehicle.maker, vehicle.model, vehicle.year);
  if (hit) return { value: hit.복합연비, source: 'public_api', trust: 'A' };

  const est = await llmEstimate(vehicle);   // §4.4
  return { value: est.kmpl, source: 'ai_estimate', trust: 'C', reasoning: est.why };
}
```

### 4.4 AI 유사차량 추정

공공 DB에 없는 차량의 기준연비를 LLM이 추론한다.

**입력** — 제조사, 모델명, 연식, 차종, 총중량, 배기량, 연료 종류
**참조** — 공공 API에서 같은 제조사·같은 차급으로 조회한 상위 N건(있으면)
**출력** (강제 JSON)

```json
{
  "estimated_kmpl": 3.4,
  "reference_models": ["...", "..."],
  "reasoning": "동급 총중량 대형 견인차의 표시연비 범위에서...",
  "confidence": "medium"
}
```

**프롬프트 규칙**
1. 반드시 참조 모델을 제시하라. 근거 없는 숫자 금지.
2. 확신 없으면 `confidence: "low"`로 낮춰라 — 낮은 신뢰도가 틀린 숫자보다 낫다.
3. 한국 시장 판매 모델 기준으로만 추론하라.
4. 추정 범위를 벗어나면(승용 5~25 km/L, 화물 1.5~12 km/L) 반환하지 말고 `null`.

**화면 표기** — `AI 추정` 배지 클릭 시 `reasoning`과 `reference_models`가 툴팁으로 펼쳐진다. **왜 이 숫자인지 클릭 한 번에 보여야 한다.**

### 4.5 적재상태 보정

기준연비는 시험조건 값이다. 실주행 보정계수를 곱한다.

```
기준연비_공차 = 기준연비 × 1.08
기준연비_적차 = 기준연비 × 0.78
```

> **출처 등급: 설정값 (미확정)**
> 현재 화물차 데모 기준(등록증 3.6 → 공차 3.9 / 적차 2.8)을 역산해 맞춘 계수다.
> 승용차는 적재상태 구분이 없으므로 보정 없이 기준연비를 그대로 쓴다.
> PoC 1차 과제로 차량군 실측 회귀 전환.

### 4.6 폴백 (필수)

API 실패·키 미발급·네트워크 차단 시 **`public/fixtures/fuel_economy_cache.json`** 을 읽는다. 데모에 나오는 차량의 조회 결과를 미리 캐싱해 커밋해 둔다.

**당일 아침에 캐시 파일부터 만든다.** 발표 중 API가 죽어서 기준연비가 안 나오면 Safe 화면 전체가 무너진다.

---

## 5. 판정 로직 — v1에서 변경 없음

**이 장은 한 줄도 바꾸지 않았다.** 재구현 시 숫자·조건 순서까지 그대로 옮긴다.

### 5.1 정규화

```
발생률(건/km) = (급가속+급출발+급감속+급정지) / 주행거리(km)
```
과속은 제외한다(단말이 전 차량 0으로만 보고 — 미수집 상태로 판단). 공차/적차는 `laden` 플래그로 나눠 각각 산출.

### 5.2 신뢰등급 5단계 (if-else 순서 그대로)

```js
if (reported_km === 0 && core_events > 0)      grade = 'D';   // 측정 불가
else if (rate !== null && rate > 1.0)          grade = 'A';   // 센서 과민
else if (rate === 0 && reported_km > 100)      grade = 'C';   // 센서 침묵
else if (contradicts)                          grade = 'B';   // 센서 둔감
else                                           grade = '정상';
```
`contradicts` = `signal_ratio < 0.6 && rank_inversion >= 2`

| grade | label | tone | verifiable | settle |
|---|---|---|---|---|
| 정상 | 정상 | ok(teal) | true | allow |
| A | 센서 과민 | warn(amber) | false | conditional |
| B | 센서 둔감 | caution(amber) | false | conditional |
| C | 센서 침묵 | dead(rose) | false | block |
| D | 측정 불가 | void(steel) | false | block |

### 5.3 연료 교차검증

```
baseline_fuel_l = Σ(공차 leg거리 / 기준연비_공차) + Σ(적차 leg거리 / 기준연비_적차)
                                                    ↑ §4에서 조회한 값 (v2 변경점)
drive_fuel_l    = Σ(leg 연료 − 공회전분)     # 공회전분 = idle_sec/3600 × IDLE_L_PER_HOUR

fuel_excess       = drive_fuel_l / baseline_fuel_l − 1
fuel_penalty      = 1 − 1/(1+fuel_excess)
fuel_implied_rate = max(0, (fuel_penalty / FUEL_PENALTY_MAX) × FUEL_PENALTY_RATE_SCALE)

signal_ratio      = rate / fuel_implied_rate
rank_inversion    = fuel_rank − observed_rank
```

| 상수 | 값 |
|---|---|
| `IDLE_L_PER_HOUR` | 2.4 L/h |
| `FUEL_PENALTY_MAX` | 0.12 |
| `FUEL_PENALTY_RATE_SCALE` | 0.3 |
| `FAINT_RATIO_THRESHOLD` | 0.6 |
| `RANK_INVERSION_MIN` | 2 |

> `FUEL_KMPL_EMPTY` / `FUEL_KMPL_LADEN` 상수는 **삭제**한다. §4의 조회 결과로 대체.

### 5.4 물리 정합성 검사 R2~R5 + F1·F2

```
R2 물리정합성  평균속도(이동구간만, 간격>600초는 정차로 skip) > 110 km/h
R3 속도적분    |Σ((prev+cur)/2 × Δt) − Σ(Δodo)| / Σ(Δodo) > 15%
R4 시퀀스중복  [lat,lon,speed,rpm] 튜플 5행 이상 연속 완전 일치
R5 연속성      Δodo < −0.05 (역행) 또는 순간환산속도 > 150 km/h (점프)

F1  measured_kmpl > 기준연비(§4)  →  조작 의심
F2  |제출거리 − 배차거리| / 배차거리 > 10%  →  조작 의심
```

⚠️ **v2 주의** — 실제 통합단말기 데이터가 2분 간격이면 R3(속도적분)·R4(시퀀스중복)의 검출력이 크게 떨어진다. 10초 간격 합성 데이터에서는 잘 돌지만 실데이터에서는 약해진다. **화면에 "샘플링 간격 N초" 를 표시하고, 120초 이상이면 R3/R4를 "판정 보류"로 표기**한다. 못 잡는 걸 잡은 척하지 않는다.

**최종 판정 사다리**
```
grade C/D              → 신뢰 불가
R2~R5 또는 F1/F2 발동   → 조작 의심
grade A                → 신뢰 불가
grade 정상             → 통과
그 외                  → 판정 불가
```

### 5.5 배출량 · Tier

```
연료(L) = Σ(구간거리 ÷ 적재상태별 기준연비) + 공회전시간 × 2.4 L/h
CO₂(kg) = 연료(L) × 2.606                  # 경유 배출계수, 설정값
원단위   = CO₂(g) ÷ 톤킬로                  # 40ft 30.5t / 20ft 21.0t
```
표준 배출원단위(Tier 1 비교용) = 62.0 gCO₂/ton-km

`tierOf(v) = v.fuel_l > 0 ? 3 : 1`
1차 데이터 비율 = Tier 3 이상 배출량 ÷ 총 배출량 (ISO 14083)

### 5.6 구간 귀속 검증 — **v2에서 단순화**

**지오펜스 단독으로 판정한다.** v1의 3중 대조(배차 시각 / 지오펜스 / 터미널 반출입)에서 배차·터미널 축을 뺀다.

```
설정(§3.5)에 등록된 사업장 좌표 · 반경 1km
   ↓
GPS 궤적이 원에 진입/이탈한 시각을 계산
   ↓
출발 사업장 이탈 시각 → 도착 사업장 진입 시각 = 귀속 구간
```

**⚠️ 2분 샘플링 때문에 반드시 이렇게 구현할 것**

점 포함 판정(`거리(점, 중심) < 반경`)을 쓰면 **안 된다.** 시속 80km에서 2분이면 2.7km를 이동하므로, 반경 1km 원을 통째로 건너뛰어 진입이 검출되지 않는다.

```
❌ 점 포함 판정:  dist(p_i, center) < R
✅ 선분 교차 판정: segment(p_i, p_i+1) 이 원과 교차하는가
                  교차하면 교점까지 선형 보간해 진입/이탈 시각 추정
```

진입/이탈 시각은 **추정값**이므로 화면에 `±샘플링간격/2` 오차를 함께 표기한다.

**판정**
```
출발·도착 사업장 모두 교차 검출  →  verified
한쪽만 검출                     →  partial (증명서에 "부분 검증" 표기)
둘 다 미검출                    →  failed
```

### 5.7 Heat-map 구간 집계

§3.3 참조. v1 §4.7과 동일.

### 5.8 차량 평가 점수 (1000점)

```
종합점수 = round( (신뢰율/100)×500 + (운전점수/100)×300 + (정비이행률/100)×200 )
```
밴드: 700~1000 우수 / 400~699 보통 / 0~399 주의

톤 임계값: `trustTone` <50 dead <80 warn / `scoreTone` <60 dead <85 warn / `maintTone` <60 dead <90 warn

차량 리포트는 단말 grade가 정상이 아니면 운전습관 문구를 "단말기 이상으로 판단 어려움"으로 덮어쓴다.

> **v2 배치 변경** — 이 점수는 Safe 화면과 기사뷰에만 나온다. 증명서에서는 제거(§3.4).

### 5.9 개인 S&E 점수 · 배지 · 정비 자기신고

```
scoreOf(v) = v.verifiable && v.rate !== null ? round(100 − min(v.rate, 1) × 70) : null
badgeTierOf: null→none / ≥90 gold / ≥75 silver / ≥60 bronze / else none
```
정비 자기신고 2항목(엔진오일·타이어 교체). `ratio ≥ 1.0 초과 / ≥ 0.8 곧 필요 / else 양호`. source는 항상 `'self'`(신뢰등급 D).

---

## 6. 데이터 스키마

### 6.1 원천 CSV (`files2/`, 빌드 스크립트만 읽음)

v1과 동일. `vehicle_master.csv` / `daily_summary.csv` / `trip.csv` / `leg.csv` / `event.csv` / `dtg_track.csv` / `_truth.csv`(**로드 금지**, 파일명 정규식 `/_?truth\.csv$/i`로 강제 차단).

**v2 추가 컬럼** — `vehicle_master.csv`에 다음 4개를 더한다.
```
maker, model, displacement_cc, registered_kmpl, vehicle_class
```
- `registered_kmpl` — 자동차등록증 기재 공인연비. 없으면 빈 값 → §4 조회 경로로 진입.
- `vehicle_class` — `'truck'` | `'car'`. 기준연비 조회 순서와 적재상태 보정 적용 여부를 가른다.

**행 수는 23**(트랙터 13 + 승용 10). `daily_summary.csv`·`event.csv`·`dtg_track.csv`도 23대 전체를 포함해야 한다 — 승용차도 통합단말기가 달려 있으므로 이벤트·궤적 데이터가 있다.

### 6.2 빌드 파이프라인 (`npm run build:data`)

```
resolveBaselineFuel()   ── vehicle_master → 공공API/캐시/AI추정 → baseline_fuel.json   ★신규
  └ buildVehicles()     ── 등급판정(§5.2) + 연료교차검증(§5.3)
buildCertificates()     ── trip/leg/event.csv + 등급
buildTracks()           ── dtg_track.csv 스트리밍, 노선별 대표 1건
buildAttribution()      ── 설정 사업장 좌표 + 궤적, 선분교차 지오펜스(§5.6)
buildCorridorHotspots() ── verifiable 이벤트만 구간 집계
```

**삭제** — `buildFleet()`, `buildGlovisFleet()`, `fleet.json`, `fleet_summary.json`, `glovis_fleet.json`, `glovis_fleet_summary.json`, `scripts/lib/fleetGen.mjs`

더미 차량 생성 로직이 통째로 사라진다. 실측 23대뿐이므로 `vehicles.json` 하나가 모든 화면의 원천이다. 결정적 PRNG·시드 매칭도 필요 없다.

### 6.3 신규 산출 JSON

**`baseline_fuel.json`**
```
{ [vehicle_id]: {
    kmpl: number,
    source: 'registration' | 'public_api' | 'ai_estimate' | 'fixture',
    trust: 'A' | 'C',
    kmpl_empty: number, kmpl_laden: number,
    reference_models?: string[], reasoning?: string, confidence?: string,
    fetched_at: string
} }
```

**`settings.json`** (기본값 시드, 사용자가 바꾸면 localStorage가 우선)
```
{ sites: Site[], corridors: Corridor[] }
```

### 6.4 변경되는 필드

`Vehicle` 타입에 추가: `vehicle_class: 'truck' | 'car'`, `scope: 1 | 3`, `baseline` (위 `baseline_fuel.json` 항목 전체)
`FleetVehicle` 타입은 **삭제** — 더미 차량이 없으므로 `Vehicle` 하나로 통일한다.
`Certificate` 타입에서 제거: `driver_id`, `carrier_only.fuel_l`, `carrier_only.idle_sec`, 점수 관련 일체

---

## 7. LLM 사용처 (v2에서 3곳)

| # | 용도 | 화면 | 실패 시 |
|---|---|---|---|
| 1 | 이기종 스키마 매핑 | 데이터 업로드 | 수동 드롭다운 매핑으로 진행 |
| 2 | **유사차량 기준연비 추정** | Safe (백그라운드) | 캐시 파일 |
| 3 | **구간 위험요소 평가·조언** | Heat-map | 문구 생략, 숫자만 표시 |

**공통 규칙**
- 강제 구조화 출력(`responseSchema`), 자유 텍스트 정규식 파싱 금지
- `attempts: 1`, `timeout: 30000` (SDK 기본 재시도 끔 — 503 시 1분 넘게 멈춘 것처럼 보임)
- 8초 넘으면 "오래 걸리는 중" 안내 + 다음 단계로 넘어갈 수 있는 버튼 항상 활성
- **모든 LLM 출력은 화면에 "AI 추정" 배지가 붙는다**

### 7.1 표준 스키마 (14필드, v1 유지)

`timestamp` `vehicle_id` `lat` `lon` `location_text` `speed_kmh` `rpm` `odo_km` `laden` `gps_status` `event_type` `accel_raw` `fuel_l` `idle_sec` `driver_id`

---

## 8. 디자인 토큰 — v1에서 변경 없음

```css
--color-ink: #06121f;    --color-ink-deep: #040d17;
--color-panel: #0c1e2e;  --color-panel-2: #0a1826;
--color-line: #193347;   --color-rule: #12293a;
--color-paper: #e9f2f7;  --color-slate: #6f8ea6;
--color-dim: #4a6b82;    --color-steel: #42627c;
--color-mist: #93aec2;   --color-chalk: #b9cedc;
--color-teal: #22c4af;   --color-amber: #e9a13b;   --color-rose: #e8556d;
```

색의 의미 (신뢰등급 전용, 장식 금지)
- **teal** = 정상 · 이행검증 성립 · 출처등급 A
- **amber** = A/B 등급 · 조건부 · **AI 추정 출처**
- **rose** = C 등급 · 센서 미작동 의심 — 이 앱의 signature 컬러
- **steel** = D 등급 · 정규화 분모 없음

폰트: `Pretendard`(본문) / `IBM Plex Mono`(`.num`, tabular-nums)

톤 클래스: `.tone-ok/.tone-warn/.tone-caution/.tone-dead/.tone-void` × `.tone-fg/.tone-bd/.tone-bg/.tone-rail`. **컴포넌트에 하드코딩 hex 금지** — grep으로 `#[0-9a-f]{3,6}` 검색해 컴포넌트 파일에 없어야 한다.

---

## 9. 구현 순서

| 트랙 | 포함 | 예상 | 자를 수 있나 |
|---|---|---|---|
| **0. 데이터 파이프라인** | csv.mjs, constants.mjs, build-vehicles, build-certificates | 60~90분 | **불가** |
| **1. 기준연비 조회** ★ | 공공API 클라이언트, 캐시 픽스처, AI 추정, `baseline_fuel.json` | 45분 | **불가** — Safe가 이걸 전제 |
| **2. Safe 화면** | `SafeScreen` 단일 표 + 상세 패널 + 순위 스트립 (실측 23대) | 75~90분 | **불가(최우선)** |
| **3. 앱 셸 · 좌측 메뉴 · 토큰** | `App.tsx`, `Sidebar.tsx`, `index.css` | 40분 | 불가 |
| **4. 설정 화면** | `SettingsScreen`, Site/Corridor 모델, localStorage | 45분 | 불가 — 증명서 구간검증이 의존 |
| **5. 증명서** | `CertificateScreen` + 구간귀속 블록 + 대비보기 | 75분 | 가능(대비보기 생략 시 55분) |
| **6. Eco 화면** | `EcoScreen` 골격 + 배출량 표 | 45분 | 가능 |
| **7. Heat-map** | 구간 집계 + SVG 폴백 지도 + 랭킹 리스트 | 45분 | 가능 |
| 7b. 카카오맵 연동 | *별도 담당자* | — | — |
| **8. 로그인** | `LoginScreen` (역할 2단으로 축소) | 25분 | 가능 |
| **9. 데이터 업로드(LLM)** | 표준스키마, 서버 미들웨어, 검토화면 | 60분 | 가능 |
| **10. 기사뷰** | v1 이식 (실측 23대에서 선택, 가짜 이력 생성 없음) | 35분 | 가능 |
| **11. 공지·단말점검 루프** | `notices.ts`, `deviceRequests.ts` | 40분 | 가능 |
| **12. 조작탐지 데모** | 픽스처 2세트, F1/F2, `IntegrityCompareScreen` | 60분 | 가능 |
| **13. Heat-map AI 평가** | LLM 구간 조언 | 30분 | 가능 |

**자르는 순서: 13 → 12 → 11 → 10 → 9 → 7 → 6 → 5**
**0·1·2·3·4는 절대 자르지 않는다.** Safe 화면이 없으면 이 프로젝트의 논지 자체가 성립하지 않는다.

---

## 10. 당일 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| 공공API 활용신청 승인 지연 | Safe 화면 전체 정지 | **09:00에 제일 먼저 신청.** 캐시 픽스처를 미리 만들어 커밋 |
| 대형 화물차가 API DB에 없음 | 기준연비 조회 실패 | 예상된 동작. 등록증 → AI 추정 경로로 자동 진입(§4.3) |
| 카카오맵 담당자 진도 지연 | Heat-map·설정 지도 공백 | 두 화면 모두 폴백 필수(SVG 지도 / 좌표 직접입력) |
| 2분 샘플링에서 지오펜스 미검출 | 증명서 구간검증 전부 failed | **선분 교차 판정으로 구현**(§5.6). 점 포함 판정 금지 |
| LLM 3곳 동시 사용으로 쿼터 소진 | 발표 중 실패 | 기준연비는 빌드 타임에 미리 조회해 JSON으로 굳힘 |
| 화주뷰 삭제로 발표 서사 붕괴 | 심사위원 혼란 | 발표 스크립트를 "우리가 원청에 증명한다"로 재작성 필요 ⚠️ |
| 23대로 줄어 순위 변동 임팩트 감소 | 데모 하이라이트 약화 | 13대 실측 분포(과다검출3/미세검출4/무검출4/계측불능2)를 전면에. "이건 실제 데이터"가 212대 시뮬레이션보다 강하다 |
| Scope 1·3 혼합 집계 | 온실가스 회계 오류 지적 | Eco 총계를 Scope별 분리(§3.2). 승용차 원단위는 gCO₂/km |
| 실측 차량번호 노출 | 사내정보 유출 | 데모 영상·배포본 모두 익명화. `SB-0000xx` 네임스페이스로 치환 |
